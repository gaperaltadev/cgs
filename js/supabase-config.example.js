// Template para desarrollo local
// Copiar como js/supabase-config.js y completar con tus claves
// (js/supabase-config.js está en .gitignore — nunca commitear las claves reales)
window.SUPABASE_URL  = '';   // Settings > API > Project URL
window.SUPABASE_ANON = '';   // Settings > API > anon public
